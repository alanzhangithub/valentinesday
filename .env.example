# Supabase Configuration
# Get these from your Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key for server-side operations (keep secret!)
# Only used in API routes, never exposed to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here-generate-with-openssl-rand-base64-32

# Google OAuth Credentials
# Get these from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Whitelisted Emails (only these 2 emails can access the site)
ALLOWED_EMAIL_1=meedo@example.com
ALLOWED_EMAIL_2=beedo@example.com

# Site Password (bcrypt hash)
# Generate hash: npx bcryptjs hash "your-password"
# Or use: node -e "require('bcryptjs').hash('your-password', 10).then(console.log)"
SITE_PASSWORD_HASH=$2a$10$your-bcrypt-hash-here

# Resend API key - get one at https://resend.com
RESEND_API_KEY=re_xxxxxxxxxx

# Email sender address (must be verified in resend)
EMAIL_FROM=Meedobeedo <noreply@yourdomain.com>

# Admin email - where wish notifications and stuff go
ADMIN_EMAIL=admin@yourdomain.com

# App URL - for links in emails
NEXT_PUBLIC_APP_URL=https://meedobeedo.com
